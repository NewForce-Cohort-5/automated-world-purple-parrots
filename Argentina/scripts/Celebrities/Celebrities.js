export const countryCeleb = (celebrityCollection) => {
    return `
        <section class= "celeb card">
            <div class="celeb_name">${celebrityCollection.name}</div>
        </section>
    `
}
