export const countryCity = (cityCollection) => {
    return `
        <section class= "city_card">
        <ul>
            <li>    
            <div class="city_name">${cityCollection.city}</div>
            </li>
        </ul>
        </section>
    `
}